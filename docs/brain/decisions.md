---
tags: [journal, decisions]
---
# Decisions

## 2026-06-16

- Work in the real Multitasker repo: `C:\Users\Kyrylo\multitasker`.
- Push work to `origin` on GitHub so progress survives context loss.
- Use `apps/agent-room/` as the active Agent Room implementation.
- Keep Pixel Agents upstream code mostly isolated in `core/` and `webview-ui/`.
- Put Multitasker-specific integration code in `apps/agent-room/adapter/`.
- Exclude the old local `assets/agent-room/` prototype from GitHub.
- Do not generate new art for the current integration unless the owner explicitly asks.
- Log each stage in `docs/brain/state.md`.
